# Fase 6 — Rascunho da proposta ao Devs Norte

Status: **rascunho, não enviado**. Pivô: em vez de piloto, a proposta é expor a vitrine (`/`) na plataforma do Devs Norte (link, card ou iframe).
Completar os [colchetes] com dados reais do piloto.

---

**Título:** Unificar inscrição, ingresso e check-in dos eventos em uma ferramenta só

Oi, pessoal! Vi que existem `cosmopolitan`, `ingressos` e o fork de `pretix` tentando
resolver o mesmo problema. Construí [nome] e operei o check-in do [meetup X] em [data].

**Resultado do piloto:** [N] inscritos, [N] check-ins, [tempo médio] por pessoa,
[N] falhas de leitura, [o que quebrou e como corrigi].

**O que faz:** inscrição pública, ingresso com QR por e-mail, check-in pela câmera do
celular, dashboard ao vivo (Phoenix LiveView, Postgres, deploy no Fly).

**Proposta:** [escolher uma]
1. Manter como projeto satélite em `devsnorte/ecossistema`
2. Absorver o que falta do `cosmopolitan`/`ingressos` aqui
3. PR de substituição nos repos existentes

Fora de escopo por ora: pagamento, múltiplos tipos de ingresso, lista de espera, certificado.

Quero feedback antes de mexer em qualquer repo de vocês.
